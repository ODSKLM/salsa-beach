package com.odsklm.salsabeach.types

import java.nio.charset.Charset

package object rowconverters {
  private[types] val UTF8_ENCODING = "UTF-8"
  private[types] val UTF8_CHARSET = Charset.forName(UTF8_ENCODING)
}
